{% extends 'base.html.twig' %}

{% block title %}TaskAssignment{% endblock %}

{% block body %}
    <h1>TaskAssignment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_assignment.id }}</td>
            </tr>
            <tr>
                <th>Task</th>
                <td>{{ task_assignment.getTaskId.getTitle }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ task_assignment.getUserId.getName }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ task_assignment.createdAt ? task_assignment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_assignment_index') }}">back to list</a>

    <a href="{{ path('app_task_assignment_edit', {'id': task_assignment.id}) }}">edit</a>

    {{ include('task_assignment/_delete_form.html.twig') }}
{% endblock %}
